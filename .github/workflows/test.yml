name: Test
on: push
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
        with:
          fetch-depth: 2
      
      - name: Check has changes
        id: check-changes
        uses: rdietrick/did-files-change@v1.0
        with:
          pathspec: .

      - name: Debug
        run: |
          echo "Has changes: ${{ steps.check-changes.outputs.changed }}"

      - name: Test
        if: steps.rcheck-changes.outputs.changed == '1'
        run: |
          echo "Files did change!"

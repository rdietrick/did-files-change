name: Test
on:
  push:
    branches:
      - 'test-workflow'
      
jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 2
      
      - name: Check has changes
        id: check-changes
        uses: rdietrick/did-files-change@test-workflow
        with:
          sha: ${{ github.sha }}
          pathspec: test

      - name: Debug
        run: |
          echo "Has changes: ${{ steps.id.check-changes.outputs.changed }}"